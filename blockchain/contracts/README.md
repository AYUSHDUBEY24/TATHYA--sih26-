# contracts/

Solidity source files will live here. The `DocumentIntegrity` hash-anchoring
contract (registerDocumentHash / getDocumentHash) is intentionally **not**
implemented yet — it arrives in Phase 7 of the implementation plan
(see `docs/implementation-plan.md`).
